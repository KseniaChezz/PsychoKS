<%= form_with(model: board_in_project, local: true) do |form| %>
  <% if board_in_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(board_in_project.errors.count, "error") %> prohibited this board_in_project from being saved:</h2>

      <ul>
      <% board_in_project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :board_id %>
    <%= form.text_field :board_id %>
  </div>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.number_field :project_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
